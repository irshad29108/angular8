import { TfnFilterPipe } from './tfn-filter.pipe';

describe('TfnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TfnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
