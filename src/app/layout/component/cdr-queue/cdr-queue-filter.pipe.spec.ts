import { CdrQueueFilterPipe } from './cdr-queue-filter.pipe';

describe('CdrQueueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CdrQueueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
