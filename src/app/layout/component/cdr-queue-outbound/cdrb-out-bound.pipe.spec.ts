import { CdrbOutBoundPipe } from './cdrb-out-bound.pipe';

describe('CdrbOutBoundPipe', () => {
  it('create an instance', () => {
    const pipe = new CdrbOutBoundPipe();
    expect(pipe).toBeTruthy();
  });
});
