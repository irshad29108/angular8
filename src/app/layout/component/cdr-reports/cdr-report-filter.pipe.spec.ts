import { CdrReportFilterPipe } from './cdr-report-filter.pipe';

describe('CdrReportFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CdrReportFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
