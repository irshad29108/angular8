import { PublisherFilterPipe } from './publisher-filter.pipe';

describe('PublisherFilterPipe', () => {
  it('create an instance to ', () => {
    const pipe = new PublisherFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
