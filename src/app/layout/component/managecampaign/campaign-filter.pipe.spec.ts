import { CampaignFilterPipe } from './campaign-filter.pipe';

describe('CampaignFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
